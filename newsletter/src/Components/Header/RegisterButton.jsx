export default function RegisterButton({ children }) {
    return (
        <button className="py-2 px-2 border-2 rounded-lg border-[#ce1010] hover:border-[#000]">
            {children}
        </button>
    )
}